{% extends '/layouts/default.twig' %}

{% block content %}

    <h2>Pages list</h2> <a href="/pages/new">Добавить</a>

    <ul>
        {% for page in pages %}
        <li>
            <a href="/page/{{ page.id }}">{{ page.title }}</a>
        </li>
        {% endfor %}
    </ul>

{% endblock %}